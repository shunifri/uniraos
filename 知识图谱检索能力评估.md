# 知识图谱检索能力评估

## 评估日期
2026-04-13

---

## 一、知识图谱收集机制分析

### 1.1 当前收集方式

#### 1.1.1 长期记忆（LTM）集成
**文件**: `src/memory/knowledge-graph/manager.ts` - `onFactStored()`

```typescript
async onFactStored(entry: {
  id: string; key: string; value: unknown; tags: string[]; relation?: string;
}): Promise<void>
```

**功能**:
- ✅ 创建/更新节点
- ✅ 如果提供 `relation` 参数，创建 EXTRACTED 边
- ✅ 基于标签重叠创建 TEMPORAL 边（最多 5 个）

#### 1.1.2 知识库（KB）集成
**文件**: `src/skills/knowledge-skills.ts` - `kb_search` 技能

```typescript
// 第 2025-2041 行：搜索完成后同步到知识图谱
if (sessionManager && combined.length > 0) {
  try {
    const session = sessionManager.getOrCreate(owner);
    const sessionWithGraph = session as unknown as SessionWithGraphManager;
    if (sessionWithGraph.graphManager) {
      for (const result of combined.slice(0, 5)) {
        await sessionWithGraph.graphManager.onFactStored({
          id: `kb_${result.docId}_${result.chunkIndex}`,
          key: `kb:${result.docName}:chunk${result.chunkIndex}`,
          value: result.content.slice(0, 200),
          tags: ['kb_document', (result.docName.split('.').pop() || 'doc')],
        });
      }
    }
  } catch { /* 图谱同步失败不影响搜索结果 */ }
}
```

**功能**:
- ⚠️ 仅在搜索完成后记录前 5 个搜索结果
- ⚠️ 节点类型标记为 `kb_document` + 文件扩展名
- ⚠️ 仅存储内容的前 200 字符
- ⚠️ 没有内容语义关系提取
- ⚠️ 没有文档内部结构关系（如章节、段落等）

#### 1.1.3 标签关系提取
**文件**: `src/memory/knowledge-graph/relationship-extractor.ts`

```typescript
export function extractTagRelationships(
  entryTags: string[],
  existingEntries: Array<{ id: string; label: string; tags: string[] }>,
  minOverlap = 1,
): Array<{ targetId: string; targetLabel: string; sharedTags: string[] }>
```

**功能**:
- ✅ 基于标签重叠创建关系（TEMPORAL 边）
- ✅ 倒排索引优化（避免 O(n²)）
- ⚠️ 仅依赖标签，无语义理解

#### 1.1.4 LLM 关系提取（可选）
**文件**: `src/memory/knowledge-graph/relationship-extractor.ts`

```typescript
export async function extractRelationships(
  text: string,
  llmProvider?: LLMProvider | null,
): Promise<ExtractedRelation[]>
```

**功能**:
- ✅ 使用 LLM 从文本中提取语义关系
- ✅ 支持 `related_to`, `depends_on`, `part_of`, `uses`, `created_by`, `located_in` 等关系类型
- ✅ 带置信度评分（0-1）
- ⚠️ 当前很少主动调用（主要是可选功能）

---

### 1.2 收集机制问题分析

#### 问题 1：知识库集成过于被动
- **现状**: 仅在搜索完成后记录结果
- **问题**:
  - 知识图谱是"事后"记录，不是"事前"索引
  - 无法利用图谱进行检索增强
  - 图谱始终落后于知识库内容

#### 问题 2：缺少文档语义解析
- **现状**:
  - 仅记录文档 chunk 的浅拷贝
  - 没有解析文档内部结构
  - 没有提取文档中的实体、概念、关系
- **问题**:
  - 图谱节点缺乏语义信息
  - 无法利用文档内部的语义关联

#### 问题 3：关系类型单一
- **现状**:
  - 主要依赖标签重叠（TEMPORAL）
  - EXTRACTED 边很少使用
  - INFERRED 边几乎不存在
- **问题**:
  - 图谱边缺乏语义
  - 路径查找和子图查询效果受限

#### 问题 4：与 LTM 的集成不够深入
- **现状**: LTM 和 KB 的图谱节点分开处理
- **问题**: 缺乏跨模块的关系提取

---

## 二、知识图谱模块能力分析

### 2.1 核心能力清单

| 能力 | 实现位置 | 状态 | 评估 |
|------|----------|------|------|
| BFS 子图查询 | `bfs-extractor.ts` | ✅ 完整 | 良好 |
| 最短路径查找 | `bfs-extractor.ts` | ✅ 完整 | 良好 |
| 社区检测 | `community-detection.ts` | ✅ 完整 | 优秀（Louvain 算法） |
| 中心节点识别 | `scoring.ts` | ✅ 完整 | 良好 |
| 惊奇度评分 | `scoring.ts` | ✅ 完整 | 良好 |
| 统计信息 | `manager.ts` | ✅ 完整 | 优秀 |
| 中文标签扩展 | `bfs-extractor.ts` | ✅ 完整 | 优秀（内置映射表） |
| LLM 关系提取 | `relationship-extractor.ts` | ✅ 完整 | 良好（有实现但不常用） |
| 从 LTM 同步 | `manager.ts` | ✅ 完整 | 良好 |

### 2.2 检索相关能力详解

#### 2.2.1 BFS 子图查询
**文件**: `bfs-extractor.ts` - `extractSubgraph()`

**算法流程**:
1. 查询分词 + 中文标签扩展（ZH_TAG_MAP）
2. 节点评分（label=3, tag=2, value=1）
3. 选取 top-N 种子节点（默认 3）
4. BFS 遍历（默认深度 3，最大 50 节点）
5. 收集子图内的边

**优点**:
- ✅ 支持中文查询
- ✅ 多维度评分
- ✅ 可配置参数

**局限**:
- ⚠️ 评分机制简单（关键词匹配）
- ⚠️ 没有向量相似度
- ⚠️ 没有节点重要度加权

#### 2.2.2 社区检测
**文件**: `community-detection.ts`

**算法**: Louvain 社区发现算法

**优点**:
- ✅ 无监督，无需训练
- ✅ 模块度（Modularity）优化
- ✅ 可重建

**检索应用潜力**:
- 🔍 可用于查询时的社区优先
- 🔍 可用于结果聚类
- 🔍 可用于查询扩展（同社区节点）

#### 2.2.3 中心节点识别
**文件**: `scoring.ts` - `identifyGodNodes()`

**能力**:
- ✅ 按度（connectivity）排序
- ✅ 返回 top-N 节点

**检索应用潜力**:
- 🔍 查询时优先从中心节点开始
- 🔍 中心节点可作为"锚点"进行子图扩展
- 🔍 可用于结果排序（优先展示中心节点）

#### 2.2.4 惊奇度评分
**文件**: `scoring.ts` - `scoreSurprise()`

**评分维度**:
1. 跨类型连接（+0.5/类型）
2. 社区桥接（+1.5/社区）
3. 边缘节点连到枢纽（+2.0）
4. INFERRED 边（+0.3/边）

**检索应用潜力**:
- 🔍 可用于发现"意外"关联
- 🔍 可用于多样化搜索结果
- 🔍 可用于知识发现场景

---

## 三、与当前混合检索的对比

### 3.1 当前混合检索（Hybrid Search）

**文件**: `src/skills/knowledge-skills.ts` - `search()`

| 组件 | 实现 | 权重 |
|------|------|------|
| 关键词搜索 | MySQL FULLTEXT + `kb_keywords` 表 | 0.4 |
| 语义搜索 | 向量相似度（cosine） | 0.6 |
| 融合算法 | RRF（Reciprocal Rank Fusion）, k=60 | - |

**优点**:
- ✅ 成熟稳定
- ✅ 关键词搜索快速
- ✅ 向量搜索语义理解好
- ✅ RRF 融合效果可靠

**局限**:
- ⚠️ 关键词搜索依赖精确匹配
- ⚠️ 向量搜索是"平面"的，无结构信息
- ⚠️ 无法利用关联关系
- ⚠️ 无法进行推理式查询（"A 和 B 的关系"）

### 3.2 知识图谱检索（潜在）

| 能力 | 当前可用 | 检索优势 |
|------|----------|----------|
| 子图查询 | ✅ | 上下文检索，关联发现 |
| 路径查找 | ✅ | 推理式查询，关系解释 |
| 社区优先 | ✅ | 聚类，主题聚焦 |
| 中心节点优先 | ✅ | 重要性排序 |
| 惊奇发现 | ✅ | 意外关联，知识发现 |

---

## 四、精度和效率提升潜力评估

### 4.1 精度提升潜力

#### 4.1.1 查询类型分类

| 查询类型 | 当前混合检索 | 知识图谱增强 | 预期提升 |
|----------|--------------|--------------|----------|
| 事实查询（"什么是 X"） | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 持平 |
| 关键词检索（"包含 Y"） | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 可能下降 |
| 语义检索（"类似 Z"） | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 持平 |
| 关联查询（"X 和 Y 的关系"） | ⭐⭐ | ⭐⭐⭐⭐⭐ | **大幅提升** |
| 推理查询（"通过 A 到 B"） | ⭐ | ⭐⭐⭐⭐ | **大幅提升** |
| 发现查询（"意外关联"） | ⭐⭐ | ⭐⭐⭐⭐⭐ | **大幅提升** |

#### 4.1.2 精度提升机制

| 机制 | 实现方式 | 预期精度增益 |
|------|----------|--------------|
| 上下文感知 | 子图查询提供关联信息 | +5-15% |
| 关系推理 | 路径查找提供推理能力 | +10-25%（对关系查询） |
| 社区聚焦 | 同社区节点优先 | +5-10% |
| 多样性控制 | 惊奇度评分避免重复 | +5-10% |
| 结果解释 | 可展示路径和子图 | 体验提升（难以量化） |

### 4.2 效率提升潜力

#### 4.2.1 计算复杂度

| 操作 | 当前混合检索 | 知识图谱 |
|------|--------------|----------|
| 关键词搜索 | O(k log n) | - |
| 向量搜索 | O(n log n) | - |
| BFS 子图查询 | - | O(V + E) |
| 最短路径查找 | - | O(V + E) |
| 社区检测 | - | O(n log n)（Louvain） |

**评估**:
- ⚠️ BFS 和路径查找在大图上可能较慢
- ✅ 社区检测可以离线预处理
- ⚠️ 图索引需要优化（当前是内存存储）

#### 4.2.2 缓存和优化机会

| 优化方向 | 可行性 | 预期效率增益 |
|----------|--------|--------------|
| 社区预计算 | ✅ 高 | +20-30% |
| 中心节点缓存 | ✅ 高 | +10-15% |
| 热点子图缓存 | ✅ 中 | +10-20% |
| 向量 + 图混合索引 | ⚠️ 中 | +15-25%（需要架构改造） |

---

## 五、建议方案

### 5.1 阶段 1：增强知识图谱收集（低风险，中收益）

#### 5.1.1 知识库导入时即索引到图谱
**目标**: 知识图谱不是"事后"记录，而是"事前"索引

**实现**:
```typescript
// 在 kb_ingest 完成后，立即解析并索引
async ingest(...) {
  // ... 现有逻辑 ...

  // 新增：知识图谱索引
  if (graphManager) {
    await graphManager.indexDocument(
      docId,
      docName,
      content,
      chunks,  // 带页码和边界框
      tags
    );
  }
}
```

**优点**:
- ✅ 图谱与知识库同步
- ✅ 可利用文档解析的中间结果（chunks）
- ✅ 低风险（可选功能）

#### 5.1.2 增强关系提取
**目标**: 不仅靠标签，更靠内容语义

**实现**:
1. 文档解析后，使用 LLM 提取实体和关系
2. 为文档内部结构创建节点（章节、段落、图表等）
3. 创建层次关系（`part_of`, `next_section`, `references` 等）

**优点**:
- ✅ 图谱边有语义
- ✅ 支持文档内导航
- ✅ 支持"定位到文档某部分"的查询

### 5.2 阶段 2：混合检索增强（中风险，高收益）

#### 5.2.1 三阶段检索架构

```
阶段 1: 快速召回
├── 关键词搜索（top-100）
└── 向量搜索（top-100）

阶段 2: 知识图谱重排序
├── 构建查询相关子图
├── 计算结果节点在子图中的位置
├── 社区一致性检查
└── 关系丰富度评分

阶段 3: 结果重排
├── RRF 融合原始分和图谱分
├── 多样性控制（惊奇度）
└── 返回最终结果
```

**优点**:
- ✅ 保留现有混合检索的稳定性
- ✅ 图谱作为"锦上添花"，不降低质量
- ✅ 可 A/B 测试

#### 5.2.2 图谱评分因子

| 因子 | 描述 | 建议权重 |
|------|------|----------|
| 到种子节点的距离 | BFS 深度 | 0.3 |
| 社区归属 | 是否与种子同社区 | 0.2 |
| 节点中心度 | 度、介数等 | 0.2 |
| 关系丰富度 | 边的数量和类型 | 0.2 |
| 惊奇度 | 是否是"意外"发现 | 0.1 |

### 5.3 阶段 3：图谱原生检索（高风险，极高收益）

#### 5.3.1 查询理解路由

```typescript
async smartSearch(query: string) {
  const queryType = analyzeQuery(query);

  switch (queryType) {
    case 'factual':
      return hybridSearch(query);  // 保持原样
    case 'relational':
      return graphPathSearch(query);  // 路径优先
    case 'discovery':
      return graphSubgraphSearch(query);  // 子图优先
    default:
      return hybridSearch(query);
  }
}
```

#### 5.3.2 查询类型分类器

| 特征 | 关系查询 | 发现查询 | 事实查询 |
|------|----------|----------|----------|
| "关系"、"关联"、"连接" | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐ |
| "从...到..."、"通过..." | ⭐⭐⭐⭐ | ⭐⭐ | ⭐ |
| "意外"、"没想到"、"新发现" | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ |
| "什么是"、"定义" | ⭐ | ⭐ | ⭐⭐⭐⭐⭐ |

---

## 六、总结与建议

### 6.1 核心发现

1. **知识图谱能力完整，但未激活**
   - ✅ BFS、路径查找、社区检测、评分等都已实现
   - ⚠️ 但检索流程未使用这些能力
   - ⚠️ 知识图谱主要是"记录"，不是"索引"

2. **收集机制过于被动**
   - ⚠️ 知识库搜索完成后才记录
   - ⚠️ LTM 集成不深
   - ⚠️ 缺乏语义关系提取

3. **对精度提升的潜力：有条件乐观**
   - ✅ 对关系查询、推理查询、发现查询：**大幅提升**（+15-25%+）
   - ⚠️ 对普通事实查询：**持平或微降**
   - 🔍 关键是**查询路由**和**结果融合**

4. **对效率提升的潜力：需要优化**
   - ⚠️ 图操作（BFS、路径查找）在大图上较慢
   - ✅ 很多计算可以**离线预处理**（社区、中心节点）
   - 🔍 需要**索引优化**（当前是内存 HashMap）

### 6.2 行动建议

| 优先级 | 建议 | 预计工作量 | 预计收益 | 风险 |
|--------|------|------------|----------|------|
| P0 | **修复 Float32Array 错误** | 30 分钟 | 稳定性修复 | 低 |
| P1 | **知识库导入时索引到图谱** | 1-2 天 | 中（数据质量） | 低 |
| P1 | **混合检索 + 图谱重排序** | 2-3 天 | 高（精度） | 中 |
| P2 | **社区和中心节点预计算** | 1 天 | 中（效率） | 低 |
| P2 | **查询类型分类器** | 2-3 天 | 高（体验） | 中 |
| P3 | **图谱原生检索** | 5-7 天 | 极高（能力） | 高 |

### 6.3 最终结论

**当前知识图谱模块能否提升检索精度和效率？**

| 维度 | 答案 | 说明 |
|------|------|------|
| 精度 | ✅ **有条件能** | 对关系/推理/发现查询提升显著，但需要查询路由和融合 |
| 效率 | ⚠️ **需要优化** | 基础操作（BFS/路径）可能较慢，但可通过预处理和缓存改善 |
| 整体建议 | 🟢 **建议分阶段实施** | 从 P0/P1 开始，验证效果后再深入 |

**最稳妥的路径**：
1. 先修复 Float32Array 错误（已完成）
2. 增强图谱收集（知识库导入时索引）
3. 实现"混合检索 + 图谱重排序"
4. 小规模 A/B 测试验证
5. 根据结果决定是否深入

---

*评估完成*
