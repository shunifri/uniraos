# P3 - 知识图谱原生检索 - 实施计划

## 概述
重构知识库检索体系，从混合检索（关键词+向量）升级为**知识图谱原生检索**。

## 版本管理
- **当前分支**: `master` (已保存当前状态)
- **重构分支**: `feature/p3-knowledge-graph-retrieval`
- **回退方式**: `git checkout master` 或 `git revert`

---

## 架构设计

### 三阶段检索架构
```
阶段 1: 查询理解与路由
├── 查询分词和预分析
├── 查询类型分类器
│   ├── 事实查询 → 混合检索
│   ├── 关系查询 → 图谱路径检索
│   ├── 发现查询 → 图谱子图检索
│   └── 混合查询 → 图谱增强检索
└── 查询扩展（同义词、相关概念）

阶段 2: 多源检索执行
├── 混合检索 (关键词+向量) - 作为基础
├── 图谱子图检索 (BFS)
├── 图谱路径检索 (最短路径)
└── 图谱社区检索 (社区优先)

阶段 3: 结果融合与排序
├── RRF 融合各检索源结果
├── 图谱位置评分（种子距离、社区归属）
├── 图谱关系评分（边的类型和权重）
├── 惊奇度评分（多样性控制）
└── 结果去重与排序
```

---

## 实施步骤

### 阶段 1: 查询类型分类器（优先级：高）

#### 1.1 创建查询分类器
**文件**: `src/skills/knowledge-skills.ts` 或新建 `src/skills/query-classifier.ts`

```typescript
// 查询类型枚举
type QueryType = 'factual' | 'relational' | 'discovery' | 'hybrid';

interface ClassifiedQuery {
  type: QueryType;
  confidence: number;
  keywords: string[];
  entities: string[];
  relations: string[];
}

function classifyQuery(query: string): ClassifiedQuery {
  // 规则引擎 + 关键词匹配
  const indicators = {
    factual: ['什么是', '定义', '介绍', '说明', 'how to', 'what is'],
    relational: ['关系', '关联', '连接', '从...到', '通过', '路径', '区别', '相同'],
    discovery: ['意外', '没想到', '新发现', '探索', '发现', '有什么', '还有什么'],
  };

  // 匹配和评分
  // 返回分类结果
}
```

#### 1.2 集成到 kb_search
修改 `kb_search` 技能的 handler，增加查询分类。

---

### 阶段 2: 知识库导入时索引到图谱（优先级：高）

#### 2.1 修改 KnowledgeBase.ingest
**文件**: `src/skills/knowledge-skills.ts`

```typescript
// 在 ingest 方法中，解析完成后立即索引到知识图谱
async ingest(...) {
  // ... 现有逻辑 ...

  // 新增：知识图谱索引
  if (this.graphManager) {
    await this.indexDocumentToGraph(
      docId,
      docName,
      content,
      chunksWithMeta,
      tags
    );
  }
}

// 新增方法
async indexDocumentToGraph(
  docId: string,
  docName: string,
  content: string,
  chunks: Array<{ content: string; pageNumber: number | null; bboxes: any[] }>,
  tags: string[]
) {
  // 1. 创建文档节点
  const docNode = await this.graphManager.getStore().addNode({
    id: `doc_${docId}`,
    label: docName,
    type: 'kb_document',
    tags,
    properties: { docId, contentPreview: content.slice(0, 500) },
    createdAt: Date.now(),
  });

  // 2. 创建章节/段落节点（可选，基于 chunks）
  // 3. 创建层级关系（part_of, next_section）
  // 4. 创建标签关系
}
```

#### 2.2 增强关系提取
**文件**: `src/memory/knowledge-graph/relationship-extractor.ts`

- 主动调用 LLM 提取文档中的实体和关系
- 不仅靠标签，更靠内容语义

---

### 阶段 3: 图谱检索能力（优先级：高）

#### 3.1 实现 `graphSearch` 方法
**文件**: `src/skills/knowledge-skills.ts`

```typescript
async graphSearch(
  query: string,
  opts?: {
    limit?: number;
    maxDepth?: number;
    maxNodes?: number;
  }
): Promise<Array<{
  docId: string;
  docName: string;
  chunkIndex: number;
  content: string;
  score: number;
  matchType: 'graph_subgraph' | 'graph_path' | 'graph_community';
  graphContext?: {
    path?: GraphNode[];
    subgraph?: { nodes: GraphNode[]; edges: GraphEdge[] };
    community?: number;
  };
}>> {
  // 1. 使用 querySubgraph 查询相关子图
  // 2. 从子图中提取相关的 kb_document 节点
  // 3. 映射回原始 chunk 和内容
  // 4. 计算图谱评分（位置、关系、社区等）
}
```

#### 3.2 实现 `pathSearch` 方法
```typescript
async pathSearch(
  source: string,
  target: string,
  opts?: { maxDepth?: number }
): Promise<{
  found: boolean;
  path?: Array<{ node: GraphNode; edge?: GraphEdge }>;
  explanation?: string;
}> {
  // 1. 查找源节点和目标节点
  // 2. 使用 getPath 查找最短路径
  // 3. 生成解释文本
}
```

---

### 阶段 4: 三阶段结果融合（优先级：中）

#### 4.1 实现 `hybridSearchWithGraph`
**文件**: `src/skills/knowledge-skills.ts`

```typescript
async hybridSearchWithGraph(
  query: string,
  opts?: { limit?: number; threshold?: number }
): Promise<Array<{
  docId: string;
  docName: string;
  chunkIndex: number;
  content: string;
  score: number;
  matchType: 'keyword' | 'semantic' | 'graph_subgraph' | 'graph_path' | 'hybrid';
  graphContext?: any;
}>> {
  const classified = classifyQuery(query);
  const limit = opts?.limit ?? 10;

  // 阶段 1: 基础检索（始终执行）
  const keywordResults = await this.keywordSearch(query, { limit: limit * 2 });
  const semanticResults = await this.semanticSearch(query, { limit: limit * 2 });

  // 阶段 2: 图谱检索（根据查询类型）
  let graphResults: any[] = [];
  if (classified.type === 'relational' || classified.type === 'discovery' || classified.type === 'hybrid') {
    graphResults = await this.graphSearch(query, { limit: limit * 2 });
  }

  // 阶段 3: RRF 融合 + 图谱增强评分
  const allResults = this.mergeAndRescoreResults(
    keywordResults,
    semanticResults,
    graphResults,
    classified.type
  );

  return allResults.slice(0, limit);
}
```

#### 4.2 RRF + 图谱增强评分
```typescript
private mergeAndRescoreResults(
  keywordResults: any[],
  semanticResults: any[],
  graphResults: any[],
  queryType: QueryType
): any[] {
  const scoreMap = new Map<string, { score: number; sources: Set<string>; item: any }>();

  // 1. RRF 融合
  const k = 60;

  keywordResults.forEach((r, i) => {
    const key = `${r.docId}_${r.chunkIndex}`;
    const rrfScore = 0.4 / (k + i + 1);
    const existing = scoreMap.get(key);
    if (existing) {
      existing.score += rrfScore;
      existing.sources.add('keyword');
    } else {
      scoreMap.set(key, { score: rrfScore, sources: new Set(['keyword']), item: r });
    }
  });

  semanticResults.forEach((r, i) => {
    const key = `${r.docId}_${r.chunkIndex}`;
    const rrfScore = 0.6 / (k + i + 1);
    const existing = scoreMap.get(key);
    if (existing) {
      existing.score += rrfScore;
      existing.sources.add('semantic');
    } else {
      scoreMap.set(key, { score: rrfScore, sources: new Set(['semantic']), item: r });
    }
  });

  // 2. 图谱增强（仅当有图谱结果时）
  if (graphResults.length > 0) {
    const graphBoost = queryType === 'relational' ? 1.5 : queryType === 'discovery' ? 1.3 : 1.1;

    graphResults.forEach((r, i) => {
      const key = `${r.docId}_${r.chunkIndex}`;
      const graphScore = (graphBoost * 0.5) / (k + i + 1);
      const existing = scoreMap.get(key);
      if (existing) {
        existing.score += graphScore;
        existing.sources.add('graph');
        existing.item.graphContext = r.graphContext;
      } else {
        scoreMap.set(key, { score: graphScore, sources: new Set(['graph']), item: r });
      }
    });
  }

  // 3. 排序并返回
  return [...scoreMap.values()]
    .sort((a, b) => b.score - a.score)
    .map(({ item, score, sources }) => ({
      ...item,
      score,
      matchType: [...sources].join('+') as any,
    }));
}
```

---

### 阶段 5: 社区和中心节点预计算（优先级：中）

#### 5.1 预计算触发器
**文件**: `src/memory/knowledge-graph/manager.ts`

```typescript
// 在 KnowledgeGraphManager 中增加
private precomputed: {
  communities: Map<number, string[]> | null;
  godNodes: GraphNode[] | null;
  lastComputed: number;
} = {
  communities: null,
  godNodes: null,
  lastComputed: 0,
};

async ensurePrecomputed(force = false): Promise<void> {
  const now = Date.now();
  if (force || !this.precomputed.communities || (now - this.precomputed.lastComputed) > 3600000) {
    // 每小时或强制刷新时重新计算
    this.precomputed.communities = (await this.getCommunities()).communities;
    this.precomputed.godNodes = await identifyGodNodes(this.store, 10);
    this.precomputed.lastComputed = now;
  }
}
```

#### 5.2 在检索中使用预计算
```typescript
// 在 graphSearch 中
await this.ensurePrecomputed();
// 使用 this.precomputed.communities 和 this.precomputed.godNodes
```

---

## 文件清单

### 新增文件
- `src/skills/query-classifier.ts` - 查询分类器（可选，可内联在 knowledge-skills.ts）

### 修改文件
1. `src/skills/knowledge-skills.ts` - 核心修改
   - `kb_search` handler
   - `KnowledgeBase.search()` 方法
   - 新增 `graphSearch()`
   - 新增 `pathSearch()`
   - 新增 `hybridSearchWithGraph()`
   - 新增 `indexDocumentToGraph()`
   - 新增 `mergeAndRescoreResults()`

2. `src/memory/knowledge-graph/manager.ts`
   - 新增预计算机制
   - 优化查询性能

3. `src/memory/knowledge-graph/relationship-extractor.ts`
   - 增强关系提取（LLM）

---

## 测试计划

### 单元测试
- 查询分类器准确性
- 图谱搜索结果相关性
- 结果融合逻辑

### 集成测试
- 不同查询类型的端到端测试
- A/B 测试（对比旧检索和新检索）

### 性能测试
- 检索延迟对比
- 内存使用对比

---

## 回退计划

如果出现问题，可以：
1. `git checkout master` - 回到重构前的状态
2. 或者 `git revert <commit>` - 回退特定提交
3. 保留分支 `feature/p3-knowledge-graph-retrieval` 作为参考

---

## 预计工作量
- 核心功能实现：3-4 天
- 测试和优化：1-2 天
- 文档和整理：0.5 天
- **总计：约 5 天**
